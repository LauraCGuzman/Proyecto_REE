# Dependencias de desarrollo: gate de regresión, serialización del modelo y
# tests. No hacen falta en el pipeline de producción (Fase 2+).
-r requirements.txt

pytest==9.1.1
nbclient==0.11.0
nbformat==5.10.4

# Fase 6 (panel de Streamlit, PR 2): tests/test_panel.py importa app/panel.py,
# que importa streamlit. El panel en producción usa su propio
# app/requirements.txt (sin scikit-learn) -- esto es solo para que
# `pytest tests/ -q` corra en un venv de desarrollo normal.
streamlit==1.62.0
matplotlib==3.11.0
