# Dependencias de runtime (src/, scripts/serializar_modelo.py excluido --
# ver requirements-dev.txt). Curado a mano, no `pip freeze`: solo lo que el
# código importa directamente, fijado a lo instalado y verificado en el
# venv del proyecto el 12/8/2026.
scikit-learn==1.9.0
pandas==3.0.3
numpy==2.5.0
joblib==1.5.3
pyarrow==24.0.0
requests==2.34.2
holidays==0.100
python-dotenv==1.2.2
